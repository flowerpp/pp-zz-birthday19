# Task List: 修复记忆长廊滑动——仅第一张显示

**Branch**: `004-fix-gallery-photos` | **Plan**: [plan.md](./plan.md) | **Spec**: [spec.md](./spec.md)

---

## Summary

| 阶段 | 任务数 | 说明 |
|------|--------|------|
| Phase 1: 基础修复（US1） | 1 | 核心一行代码修改 |
| Phase 2: 验收（US1+US2） | 3 | 目视验收：切换、进度、手势 |
| **合计** | **4** | |

**MVP 范围**：仅 US1（T001），完成后即可目视验证全部8个场景可访问。

---

## Phase 1: 核心修复

**目标**：修复场景滑动位移计算，使所有8个场景均可访问。

**独立测试标准**：完成 T001 后，在本地服务器打开页面，点击「›」7次，能依次看到全部8个场景。

- [X] T001 [US1] 将 `goToScene()` 中的 `translateX(-${index * 100}%)` 改为 `translateX(-${index * 100}vw)` 在 `js/main.js`

---

## Phase 2: 目视验收

**目标**：确认修复效果符合所有验收场景。

- [ ] T002 [US1] 目视验收：本地 `python3 -m http.server 8080`，点击「›」连续切换7次，确认场景1→8依次显示对应照片和文字（待用户验收）
- [ ] T003 [US2] 目视验收：场景切换时进度指示器「X / 8」同步更新，场景8时「继续 ✦」按钮出现（待用户验收）
- [ ] T004 [US1] 目视验收：在第8个场景点击「‹」可退回场景7，快速连续点击箭头无场景错位（待用户验收）

---

## 依赖关系

```
T001（修复代码）→ T002 → T003 → T004（均依赖T001修复生效）
```

所有验收任务（T002~T004）均需 T001 完成后进行，顺序可任意。

---

## 实现策略

本次修复极简：**1个文件，1行代码**。

| 文件 | 行号 | 修改前 | 修改后 |
|------|------|--------|--------|
| `js/main.js` | 97 | `wrapper.style.transform = \`translateX(-${index * 100}%)\`` | `wrapper.style.transform = \`translateX(-${index * 100}vw)\`` |
