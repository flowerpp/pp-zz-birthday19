# 实现规划：序幕页添加作者署名

**分支**: `002-add-author-credits` | **日期**: 2026-03-11 | **规格**: [spec.md](./spec.md)

---

## 摘要

在 `index.html` 序幕区的「开始旅程」按钮下方增加一行固定文本署名，
并在 `css/scenes.css` 中补充对应样式规则。无逻辑变更，无 JS 改动。

**实现状态**: ✅ 已完成（代码已提交至 `002-add-author-credits` 分支）

---

## 技术背景

**语言/版本**: HTML5 + CSS3（项目已有技术栈）
**主要依赖**: 无新增
**存储**: N/A
**测试**: 手动目视验收（与主项目豁免策略一致）
**目标平台**: 手机浏览器 + 桌面浏览器（同主项目）
**项目类型**: 纯静态网页，单文件修改
**性能目标**: N/A（纯静态文本，无性能影响）
**约束**: 不新增文件，仅修改 `index.html` 和 `css/scenes.css`
**规模/范围**: 2 个文件，各新增 < 10 行

---

## 宪法检查

| 门控项 | 状态 | 说明 |
|--------|------|------|
| 可读性 | ✅ 通过 | 仅新增一个 `<p>` 元素和一段 CSS 规则，逻辑自明 |
| 禁用代码生成框架 | ✅ 通过 | 无任何框架依赖 |
| 测试覆盖率 | ✅ 豁免 | 纯静态文本展示，手动目视验收 |
| TDD 合规性 | ✅ 豁免 | 无业务逻辑，目视验收替代 |
| 性能目标 | ✅ N/A | 静态文本，无性能影响 |
| 用户体验一致性 | ✅ 通过 | 署名样式遵循主项目视觉层级规范 |

---

## 项目结构

### 文档（本功能）

```text
specs/002-add-author-credits/
├── plan.md     # 本文件
├── spec.md     # 功能规格
└── tasks.md    # 任务清单（/speckit.tasks 生成）
```

### 受影响的源文件

```text
index.html          # 新增 <p class="intro-credits intro-line"> 元素
css/scenes.css      # 新增 .intro-credits 样式规则（字号、颜色、换行）
```

**结构决策**: 无新增文件，复用主项目已有 CSS 文件，利用现有 `.intro-line` 动画机制。

---

## Phase 0 研究

无需研究。技术决策全部继承主项目（001）已确立的模式：
- 样式写入 `css/scenes.css`（序幕相关 CSS 的归属文件）
- 利用已有 `.intro-line` + `fade-in-up` 动画，署名自动跟随动画序列

## Phase 1 设计

无新增数据实体、无 API 契约、无外部接口，跳过 data-model.md 和 contracts/。
